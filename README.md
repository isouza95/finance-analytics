# finance-analytics
Python + SQL finance analytics project that builds a small ETL pipeline for bank transaction CSVs, normalizes different statement formats into a consistent schema, loads data into SQLite, and analyzes monthly income vs spending in Jupyter with reproducible queries and charts.
